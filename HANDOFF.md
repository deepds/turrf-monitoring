# HANDOFF — точка входа для продолжения работы

**Обновлено:** 10.08.2026
**Для кого:** тот, кто продолжает работу без контекста предыдущего разговора.

Прочитайте этот файл целиком, затем — [docs/DECISIONS.md](docs/DECISIONS.md) и
[docs/KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md). Этого достаточно, чтобы
продолжить, ничего не сломав.

---

## 1. Что это за система

Ежедневный мониторинг стоимости поездок между Москвой, Санкт-Петербургом,
Сочи, Самарой и Казанью на горизонте 30 будущих дней.

Система заново измеряет рынок по детерминированной матрице из **15 840
логических наблюдений**, сохраняет исходные ответы источников, нормализует их,
применяет версионируемую методику и публикует витрину, в которой **любую цифру
можно раскрыть до конкретного предложения конкретного источника**.

Сбор начинается в 00:30 и идёт **до готовности, а не до срока**. Витрина
показывает последний полностью собранный день, поэтому незакрытый сегодняшний
снимок ей не мешает — а лишние часы сбора закрывают дыры, которых иначе не
закрыть. Единственный рубеж — 23:00 МСК: снимок обязан закрыться внутри своих
календарных суток.

Спецификация: `C:\Users\user\Downloads\SCOPE-R.md` (SCOPE-R 1.0, 07.08.2026).

**Главный принцип, из которого следует всё остальное:**

> Никакая неправильная, неполная или технически сомнительная цифра не должна
> молча попасть на управленческий дашборд.

---

## 2. Где что

| Что | Где |
|---|---|
| Репозиторий | `C:\Projects\turrf_monitoring` → приватный GitHub `deepds/turrf-monitoring` |
| Развёртывание, **часовое расписание** | node67 (`nemo`, 192.168.10.67), каталог `~/turrf_monitoring` |
| Дашборд / API | http://192.168.10.67:8090 · http://192.168.10.67:8091/docs |
| Развёртывание, **цикл по состоянию** | node230 (`meduza`, 192.168.10.230), каталог `~/turrf_monitoring` |
| Дашборд / API | http://192.168.10.230:8090 · http://192.168.10.230:8091/docs |
| Первая версия — **не трогать** | `C:\Projects\travel_monitoring`, стек `tco-*` на node67 |

**Основной стенд — meduza.** На нём модель сбора по готовности и живой снимок
за 09.08.

**Сбор на node67 остановлен 09.08 в 01:03.** Причина в ISSUE-5: лимитер темпа
общий на процесс, а не на кластер, и два стенда складывали свои темпы на общем
источнике. За один час параллельной работы node67 сделал 80 обращений и получил
**одно** с данными, meduza за тот же час — 326 обращений и 71 с данными.
Фиксированная рабочая точка под отказом источника не выживает, адаптивная
выживает; но пока лимитер не общий на кластер, **собирать может только один
стенд за раз**.

Первая версия оставлена работать намеренно: её исторические raw-ответы и Offers
— evidence и материал для сверки. Порты второй версии выбраны так, чтобы стеки
не конфликтовали.

### Карта кода

```text
tmo/core/          настройки, перечисления, время, деньги, идентификаторы, логи
tmo/catalog/       справочники городов и источников, профили методики (YAML)
tmo/planner/       построение матрицы наблюдений
tmo/connectors/    транспорт, MCP-клиент, Туту, РЖД, воспроизведение
tmo/normalization/ предложение источника → Offer
tmo/engine/        отбор, статистика, качество и уверенность
tmo/execution/     трёхфазное исполнение план → сеть → запись
tmo/services/      снимок, покрытие, расчёт, ворота, публикация, витрина, экспорт, golden
tmo/tasks/         Celery: расписание, сбор, обслуживание, health
tmo/api/           FastAPI, роутеры v1
frontend/src/      React + TS + Ant Design: 6 экранов
golden/            записанные ответы живых источников + эталонные значения
tests/             19 наборов, 274 теста
```

### Карта документации

| Документ | Когда читать |
|---|---|
| [PITFALLS.md](docs/PITFALLS.md) | **Перед изменением сбора или расчёта.** Подводные камни, каждый оплачен неверной цифрой |
| [DECISIONS.md](docs/DECISIONS.md) | **Перед любым изменением.** Что защищает от каких неверных цифр |
| [KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md) | **Перед продолжением работы.** Открытые дефекты |
| [PHASE0_AUDIT.md](docs/PHASE0_AUDIT.md) | Зачем всё устроено так, а не как в первой версии |
| [ARCHITECTURE.md](docs/ARCHITECTURE.md) | Слои, границы, жизненный цикл снимка |
| [CALCULATION_METHODOLOGY.md](docs/CALCULATION_METHODOLOGY.md) | Как из предложений получается цифра |
| [DATA_CONTRACT.md](docs/DATA_CONTRACT.md) | Что означает колонка и на что нельзя опираться |
| [COLLECTION_CAPACITY_ANALYSIS.md](docs/COLLECTION_CAPACITY_ANALYSIS.md) | Укладывается ли цикл в SLA и почему это не гарантия |
| [LOAD_PROFILE.md](docs/LOAD_PROFILE.md) | Чем платим за ночь: запросы, секунды, байты по замеру |
| [RUNBOOK.md](docs/RUNBOOK.md) | Эксплуатация, диагностика, инциденты |
| [ADMIN_GUIDE.md](docs/ADMIN_GUIDE.md) | Развёртывание и настройка |
| [USER_GUIDE.md](docs/USER_GUIDE.md) | Как читать дашборд |
| [ACCEPTANCE.md](docs/ACCEPTANCE.md) | Критерии Go / No-Go |
| [LIMITATIONS.md](docs/LIMITATIONS.md) | Чего система не утверждает |
| [SOURCES_PLAYBOOK.md](SOURCES_PLAYBOOK.md) | Поведение Туту и РЖД. **В конце — дополнение v2** |
| [DATA_PIPELINE_PLAYBOOK.md](DATA_PIPELINE_PLAYBOOK.md) | Где число начинает врать |
| [COLLECTION_RELIABILITY.md](COLLECTION_RELIABILITY.md) | Что ломается по ночам |
| [SNAPSHOT_TRANSFER.md](docs/SNAPSHOT_TRANSFER.md) | Перенос снимков между стендами и версии v1/v2 |

---

## 3. Фактическое состояние на 10.08.2026

### Главное

**Живой снимок рынка получен.** 09.08.2026, `READY`, покрытие 100 %, собран
полностью автоматическим циклом на meduza. Это первый снимок, прошедший все
четверо ворот на живых данных обоих источников.

| Семейство | Отвечено |
|---|---:|
| Проживание | 100 % |
| ЖД | 100 % |
| Авиа | 97,4 % |
| **Полнота** | **99,8 %** |

Витрина meduza показывает его. Красного баннера «демонстрационные данные»
больше нет.

### Состояние стендов

| Стенд | Модель | Состояние |
|---|---|---|
| meduza (node230) | цикл по состоянию | **основной**, работает, снимок 09.08 `READY`, идёт сбор 10.08 |
| node67 (nemo) | часовое расписание | **сбор остановлен** 09.08 в 01:03: `beat`, `worker`, `worker-service` погашены. База и витрина живы |
| ВМ Ubuntu (smpts) | цикл по состоянию | развёрнута 09.08, версия отстаёт от `main` |

node67 остановлен, потому что при работе вдвоём стенды складывали темпы на общем
источнике: за один час node67 сделал 80 обращений и получил 1 с данными, meduza —
326 обращений и 71 с данными. Поднять обратно:

```bash
ssh node67 "cd ~/turrf_monitoring && sudo docker compose start beat worker worker-service"
```

### Что в базе meduza

```text
снимок 1   2026-08-08   COLLECTING   0,42   брошен, старая модель
снимок 2   2026-08-05   READY  demo  1,00   демонстрационный
снимок 5   2026-08-09   READY        1,00   ← живой, показывается витриной
снимок 6   2026-08-09   READY        1,00   копия снимка 5, загружена при проверке переноса
снимок 7   2026-08-10   COLLECTING          идёт сейчас
```

Снимки 1 и 6 подлежат уборке: первый брошен, второй дублирует пятый и удваивает
объём по этой дате.

---

## 3a. Что сделано 09.08.2026: модель сбора по готовности

Прежняя модель раскладывала цикл по часам и в ночь 08.08 дала покрытие 44,7 %.
Часы выражали намерение и ничего не гарантировали: затянувшееся семейство шло
рядом со следующим, досбор работал вторым процессом со своим лимитером, расчёт
стартовал по часам и считал по дописываемым данным.

**Расписание знает один час — 00:30.** Всё остальное выдаёт диспетчер
`advance_snapshot` раз в пять минут, по состоянию снимка:

```text
нет снимка                     → OPEN
семейство не обходили ни разу  → COLLECT(семейство)
остались пропуски              → RECOVER
покрытие набрано / вышло время → CLOSE
снимок закрыт                  → IDLE
```

Ключевые свойства, каждое оплачено конкретным отказом:

| Свойство | Чем было оплачено |
|---|---|
| Аренда шага в Redis | два семейства шли одновременно, источник получал 36 потоков вместо 12 |
| Возврат брошенных наблюдений в план | autoheal перезапускал воркер, сироты копились, лечение дралось с восстановлением |
| Адаптивная одновременность | фиксированная точка 12 днём разваливала источник за девять минут |
| Досбор до результата | ночь с массовыми 503 оставляла дыры навсегда |
| Готовность по доле **отвеченных** | снимок из одних отказов имел стопроцентное покрытие |
| Срок публикации снят | матрица не помещалась в окно 01:00–09:00 на 2,3 часа |

Подробности механики — в [COLLECTION_RELIABILITY.md](COLLECTION_RELIABILITY.md),
отказы 8 и 9.

### Исправления, сделанные по ходу первой ночи

Все закрыты, все в `main`, все раскатаны на meduza.

1. **Отбор проживания брал чужую звёздность.** Матрица — город × звёздность ×
   даты, а фильтр сравнивал с объединением разрешённых профилем `{3,4,5}` и со
   звёздностью самой метрики не сравнивал: 55 253 нарушения, снимок не
   публиковался. Ворота отработали правильно.
2. **Готовность пускала снимок с наблюдениями в работе.** Отвеченных 98,35 %
   при пороге 98 %, но 31 наблюдение не имело терминального исхода, и ворота
   полноты их не пропустили.
3. **Аренда закрытия жила 15 минут при расчёте в 28–37.** Истекала на середине,
   диспетчер запускал закрытие заново — пять расчётов подряд.
4. **Повторный расчёт не убирал прежний.** 18 573 042 связи вместо миллиона,
   3,2 ГБ. Теперь один расчёт на пару «снимок + версия методики».
5. **Ворота искали сирот через `NOT IN` по всей таблице связей** — 42 минуты,
   незакрытая транзакция, заблокированная вторая финализация. Заменено на
   `NOT EXISTS`.
6. **`/dev/shm` контейнера базы — 64 МБ.** Финализация падала с `DiskFull` при
   терабайте свободного места на узле. `shm_size: 1gb`.
7. **Нижняя граница размера пачки в 20 наблюдений** делала пачку заведомо
   невыполнимой на дорогом семействе: две трети уходили в `BUDGET_EXHAUSTED`.
8. **Пачка досбора считалась по другой одновременности**, чем исполнялась.
9. **Воркер не писал структурные логи** — celery подменял корневой логгер.

### Перенос снимков между стендами

Работает и проверен на полной матрице: вкладка «Загрузка» отдаёт снимок одним
файлом и принимает такой же обратно.

| Уровень | Размер | Выгрузка | Загрузка |
|---|---:|---:|---:|
| `showcase` | 6,0 МБ | 11 с | 20 с |
| `evidence` | 370 МБ | 13 мин | около 40 мин |

Снимок 09.08 уровня `evidence` выложен для переноса на ВМ:
`https://github.com/deepds/tmo-snapshot-2026-08-09` — **репозиторий разовый и
подлежит удалению после переноса**, это вернёт занятое место в квоте LFS.

---

## 4. Что делать дальше — по порядку

### Шаг 1. Утром 10.08 снять итог ночи

Первая ночь на полностью исправленном коде. Смотреть: закрылся ли снимок сам,
без ручного вмешательства, и сколько раз срабатывали регулятор и размыкатель.

```bash
ssh node230 "cd ~/turrf_monitoring && curl -s http://localhost:8091/api/v1/market-snapshots/current"
```

### Шаг 2. Прибрать базу meduza

Копия снимка 09.08, загруженная при проверке переноса:

```bash
ssh node230 "cd ~/turrf_monitoring && sudo docker compose exec -T postgres psql -U tmo -d tmo -c \"DELETE FROM market_snapshots WHERE snapshot_date='2026-08-09' AND source_digest IS NOT NULL;\""
```

Брошенный снимок 08.08 старой модели:

```bash
ssh node230 "cd ~/turrf_monitoring && sudo docker compose exec -T postgres psql -U tmo -d tmo -c 'DELETE FROM market_snapshots WHERE id=1;'"
```

Удаление миллионов строк — минуты. Запускать при неработающем расчёте.

### Шаг 3. Обновить ВМ Ubuntu

Порядок — в [DEPLOY_UBUNTU_VM.md](docs/DEPLOY_UBUNTU_VM.md). Там же уборка
контейнеров первой версии и первичное наполнение витрины.

**Важно:** сначала остановить цикл, потому что `shm_size` пересоздаёт контейнер
базы, и пересоздание посреди расчёта оборвёт транзакцию.

### Шаг 4. Сверить шесть контрольных цен с сайтами

Процедура — [ACCEPTANCE.md §3](docs/ACCEPTANCE.md). **Главная незакрытая
проверка корректности.** Живой снимок теперь есть, то есть препятствий не
осталось. Автоматизации не подлежит.

### Шаг 5. Разобраться со звёздностью на стороне источника

Отбор починен, но остаётся вопрос: **почему Туту возвращает объекты вне
запрошенной категории**. В снимке 09.08 предложений с нулевой звёздностью
148 226. Отбор их отбрасывает, то есть цифры верны, но обращения тратятся
впустую. Проверить, применяется ли серверный фильтр `stars` и подтверждается ли
он в `meta.filters_applied`.

### Шаг 6. Закрыть оставшиеся дефекты

Актуальный список — [KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md). Незакрытыми
остаются ISSUE-1 (503 на устойчивый поток), ISSUE-3 (сверка), ISSUE-4
(soak-прогон), ISSUE-5 (лимитер на процесс, а не на кластер), ISSUE-6
(партиционирование), ISSUE-10 (загрузка снимка сегодняшней даты уводит цикл).

**Первым — ISSUE-11, жизненный цикл снимка.** За 09–10.08.2026 этот узел
отказал шесть раз на трёх стендах: опубликованный снимок дважды переоткрывался
запоздалыми задачами, годный снимок с покрытием 98,86 % не прошёл ворота из-за
180 наблюдений без исхода, у снимка в `FAILED` не осталось штатного пути
восстановления. Три предшественника из этого же узла уже закрывались
поодиночке — и узел продолжает отказывать, потому что каждый раз чинился
частный случай. Разбор и порядок работ — в самой записи.

Ближайшее из него: **выкатить `4cb3da3`** (запрет шагам цикла работать с
закрытым снимком) и **терминализовать остаток при закрытии по рубежу** — без
второго любой снимок, закрытый по времени с недобором, обречён на `FAILED`.

---
## 5. Как запустить

### Локально, без сети

```bash
python -m venv .venv && .venv/Scripts/pip install -e ".[dev]"
python -m tmo.cli init-db
python -m tmo.cli demo-snapshot --horizon 5
python -m pytest -q -m "not postgres and not live"
```

### Стек целиком

```bash
docker compose up -d --build
```

### PostgreSQL-набор тестов

```bash
TMO_TEST_DATABASE_URL=postgresql+psycopg://tmo:tmo_dev_pwd@192.168.10.67:55432/tmo pytest -m postgres
```

---

## 6. Ловушки, на которые легко наступить

1. **РЖД недоступен с рабочих станций.** Прогон, запущенный локально, молча
   даёт одноисточниковую железную дорогу. Собирать только на узле.
2. **Расчёт по локальной сети идёт в 20 раз дольше.** 1,5 часа против 235
   секунд. Не считайте полную матрицу с ноутбука.
3. **Правка `baseline_v1.yaml` после первого расчёта останавливает прогон.**
   Так задумано: изменение правила требует новой версии. Для разработки есть
   `TMO_ALLOW_METHODOLOGY_REREGISTER=true`, и он не работает, если на версию
   уже сослался расчёт.
4. **`alembic.ini` должен оставаться ASCII.** `configparser` читает его в
   системной кодировке; кириллица ломает alembic на Windows.
5. **`pypi.org` с узла недоступен.** Зеркало задаётся в `.env` через
   `TMO_PIP_INDEX_URL`, в образ не зашивается.
6. **Синтетический снимок не выбирается витриной.** Для показа живых данных
   нужен живой снимок, иначе висит красный баннер.
7. **Долгий сбор запускается `docker compose run -d`, не `exec`.** `exec` —
   клиент, привязанный к ssh-сессии: обрыв связи убивает прогон, наблюдения
   зависают в `RUNNING`, попытки не записываются.
8. **Пороги — в профиле, не в коде.** Соблазн «поправить константу» здесь
   означает незаметное переписывание истории.

---

## 7. Доступы

| Что | Значение |
|---|---|
| Дашборд meduza | http://192.168.10.230:8090 — **без аутентификации** |
| API meduza | http://192.168.10.230:8091/docs |
| Дашборд node67 | http://192.168.10.67:8090 — сбор остановлен, данные старые |
| Админ-токен | `tmo-admin-prototype-2026` в `~/turrf_monitoring/.env` — **заглушка, сменить** |
| Пароль БД | `tmo_dev_pwd` — **заглушка, сменить** |

Пользовательская аутентификация в scope SCOPE-R не входила: витрина только
читает и работает в локальной сети. Токеном закрыты только две операции,
меняющие состояние.

На node67 порты 55432 и 56379 проброшены наружу для отладки. На meduza и ВМ
они закрыты через `docker-compose.override.yml` — `ports: !override []`.
`!override` обязателен: без него Compose **добавляет** записи к списку портов
базового файла, и контейнер пытается занять порт дважды.

---

## 8. Ветки и теги

| Ветка | Содержание |
|---|---|
| `main` | Текущее состояние, модель сбора по готовности |
| `collection-by-readiness` | Та же работа отдельной веткой, влита в main |
| `phase-0-audit` … `phase-5-pilot` | По фазам, влиты в main |

Теги: `v2.0.0-phase0` … `v2.0.0-phase5`.

Работа 09.08.2026 велась в worktree
`.claude/worktrees/auto-build-analysis-0100-7271a8` и вливалась в `main` через
`git push origin HEAD:refs/heads/main`. **Локальный `main` в основной рабочей
копии при этом не двигается** — перед работой там нужен `git pull`.

---

## 9. Что считать сделанным

- [x] Phase 0 — аудит, шесть артефактов, живые замеры источников
- [x] Phase 1 — модель данных, миграция, матрица, трёхфазное исполнение, досбор
- [x] Phase 2 — методика, отбор с причинами, ворота, витрина
- [x] Phase 3 — REST v1, шесть экранов, экспорт
- [x] Phase 4 — Golden Dataset, 190 тестов, CI
- [x] Phase 5 — развёртывание, документация
- [x] **Живой снимок обоими источниками** — 09.08.2026, `READY`, покрытие 100 %
- [x] Модель сбора по готовности, перенос снимков между стендами
- [ ] Ручная сверка контрольных цен — **ISSUE-3**, теперь ничем не блокирована
- [ ] Soak-прогон — **ISSUE-4**
- [ ] Приёмка по [ACCEPTANCE.md](docs/ACCEPTANCE.md)
